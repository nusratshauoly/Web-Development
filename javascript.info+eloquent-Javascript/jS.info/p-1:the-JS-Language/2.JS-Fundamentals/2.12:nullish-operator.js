"use strict";

/*
// if the value isn't (null/undefined)

let user;
alert(user ?? "Anonymous"); // Anonymous

let user1 = "John";
alert(user1 ?? "Anonymous"); // John

let firstName = null;
let lastName = null;
let nickName = "Supercoder";

alert(firstName ?? lastName ?? nickName ?? "Anonymas"); // Supercoder

// ------ Comparison with || ------

let firstName = null;
let lastName = null;
let nickName = "Supercoder";

alert(firstName || lastName || nickName || "Anonymas"); // Supercoder

let height = 0;

alert(height || 100); // 100
alert(height ?? 100); // 0

// ❌ never use ?? with && / ||

// ✅ can use it with explicit parenthesis with (&& / ||)
let x = (1 && 2) ?? 3;
alert(x); // 2 ?? 3 => 2

*/
