"use strict";

/*
let message = "hello";
message = 123456;

let n = 1.45;

alert(1 / 0); // infinity

alert(infinity); // infinity

alert("not a number" / 2); //NaN

alert(NaN + 1); // NaN
alert(3 * NaN); // NaN
alert("Not a Number" / 2 - 1); // NaN

let str = "hello";
let str1 = "single quote also allow";
let str2 = `template literal ${str}`;

alert(`the result is ${1 + 2}`);

// boolean
let nameField = true;
let ageField = false;

let isGreater = 4 < 1;
alert(isGreater);

let age = null;

let age1;
alert(age1); // undefined

let age2 = 100;
age2 = undefined;
console.log(age2);

// ---- the typeof operator ------

typeof undefined; // "undefined"
typeof 0; // "number"
typeof 10n; // "bigint"
typeof true; // "boolean"
typeof "foo"; // "string"
typeof Symbol("id"); //"symbol"
typeof Math; // "object"
typeof null; // "object"
typeof alert; // "function"

*/

/*

// ---------------- task output -------------
let name = "Ilya";
alert(`hello ${1}`); // hello 1

alert(`hello ${"name"}`); // hello name
alert(`hello ${name}`); // hello Ilya


*/
