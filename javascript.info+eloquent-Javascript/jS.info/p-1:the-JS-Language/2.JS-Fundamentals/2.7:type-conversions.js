"use strict";

/*
// -------------- string conversion ------------
let value = true;
alert(typeof value);

value = String(value);
alert(typeof value); // string "true"

// ---------- numeric conversion ---------
alert("6" / "2"); // 3,  string are converted in numbers

let str = "123";
alert(typeof str);

let num = Number(str);
alert(typeof num); /// 123 (number)

let age3 = Number("an arbitary string inside a number");
alert(age3);

alert(Number("  123  ")); // 123
alert(Number("123z")); // NaN
alert(Number(true)); // 1
alert(Number(false)); // 0

alert(Boolean(1)); // true
alert(Boolean(0)); // false
alert(Boolean("hello")); // true
alert(Boolean("")); // false
alert(Boolean("0")); // true
alert(Boolean(" ")); // true

*/
